#lang racket
;;
;; {{package}} - {{module}}.
;;
;;
;; ~ {{author} {{email}} {{year}}.

(provide
 (contract-out))

;; ---------- Requirements

(require)

;; ---------- Internal types

;; ---------- Implementation

;; ---------- Internal procedures

;; ---------- Tests

(module+ test
  (require rackunit rackunit/docs-complete)
  ;; (check-docs '{{module}})
  (test-case "dummy first test"))